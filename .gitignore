# .NET build output
bin/
obj/
out/
artifacts/

# User-specific / IDE
*.user
*.suo
.vs/
.vscode/
.idea/
*.swp

# Rider
*.sln.iml

# NuGet
*.nupkg
packages/

# Test results / coverage
TestResults/
coverage*.json
coverage*.xml
*.coverage

# Secrets / local env
.env
*.local.json
appsettings.local.json

# OS cruft
.DS_Store
Thumbs.db

# Per-instance mock-world dirs (multi-agent testing; seeded from ./data)
data-*/

# Runtime-generated pairing id in the seed data dir (a self-generated UUID persisted
# on first run) — never a seed artifact, so it must not be committed.
data/device_id
